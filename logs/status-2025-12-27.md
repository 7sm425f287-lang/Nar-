## Session Update 2025-12-27 – Launch Sync
- VS Code Launch-Setup erneuert: Backend läuft nun explizit über `.venv`/python3.12 mit `NIRO_LLM=mock`, Frontend `npm run dev`, Compound „Full Stack“ startet beide Ports (8001/5173) via F5.
- Runbook ergänzt: Backend aktivieren per `source backend/.venv/bin/activate && NIRO_LLM=mock python3.12 -m uvicorn app:app --host 0.0.0.0 --port 8001 --reload`; Frontend `cd frontend && npm run dev`; Config bestätigt `BASE_DIR`=`repo root` → File-System-API bleibt auf Workspace begrenzt.
- Neues Startskript `launcher.command` (ausführbar) startet Backend im Hintergrund, Frontend-Devserver sowie Desktop-App (`npm start`) sequential; ermöglicht Double-Click-Boot der lokalen Suite.
- Icon-Pipeline ergänzt: Icon aus `~/Documents/Symbol-App/narphi-icon.png` via temporärem AppleScript (`set_icon.scpt`) und `osascript` auf `launcher.command` übertragen, Script danach entfernt; Launcher meldet Start mit Notification „ϕ — SARIT-EL Instanz erwacht. Eden wird kultiviert.“.
- Awakening-Update: `launcher.command` killt Blocker auf Port 5173 vor Vite-Start, erzwingt Wait-for-Port (curl loop) bevor Electron losläuft; `frontend/vite.config.ts` nutzt `strictPort: true` auf 5173.
- Electron Fix: `desktop/electron-main.ts` nutzt jetzt sicheres Electron-Import (`import * as electron`) und wirft bei fehlender Runtime; Desktop `npm run build` regeneriert dist, Dev-URL bleibt `http://localhost:5173`.
